<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:largeHeap="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".TunesActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.mesob.tunes" />
            </intent-filter>

        </activity>

        <!-- Audio playback foreground service -->
        <service
            android:name=".AudioForegroundService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- Notification action button receiver -->
        <receiver
            android:name=".NotificationActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.mesob.tunes.ACTION_PLAY" />
                <action android:name="com.mesob.tunes.ACTION_PAUSE" />
                <action android:name="com.mesob.tunes.ACTION_NEXT" />
                <action android:name="com.mesob.tunes.ACTION_PREV" />
                <action android:name="com.mesob.tunes.ACTION_LIKE" />
                <action android:name="com.mesob.tunes.ACTION_STOP" />
            </intent-filter>
        </receiver>

        <!-- Sleep timer alarm receiver -->
        <receiver
            android:name=".SleepTimerReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.mesob.tunes.ACTION_SLEEP_TIMER" />
            </intent-filter>
        </receiver>

        <!-- Compact widget (4×1 now-playing strip) -->
        <receiver
            android:name=".TunesWidget"
            android:exported="true"
            android:label="Tunes - Compact">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.mesob.tunes.WIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info" />
        </receiver>

        <!-- Large widget (4×2 now-playing card) -->
        <receiver
            android:name=".TunesWidgetLarge"
            android:exported="true"
            android:label="Tunes - Now Playing">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.mesob.tunes.WIDGET_LARGE_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_info_large" />
        </receiver>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
</manifest>
