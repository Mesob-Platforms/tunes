name = "tunes-music-app"
main = "worker/index.ts"
compatibility_date = "2024-01-01"

[site]
bucket = "./dist"

# Workers AI binding
[ai]
binding = "AI"

# Cron trigger: runs daily at midnight UTC; archive logic skips unless 7 days have passed
[triggers]
crons = ["0 0 * * *"]

